import autoBindFunc from './autoBind'
import rcAutoBindFunc from './rcAutoBind'

export const autoBind = autoBindFunc

export const rcAutoBind = rcAutoBindFunc

export default {
  autoBind,
  rcAutoBind,
}
